/*
 * Variables, Data Types, and Typing
 */

let wordData;

wordData = "Word list";

Console.log(wordData);

let subTotal = 100
let tac = subTotal * 0.07;
let totalWithTax = tax = subTotal;

let obj = {
    key1: "Value 1",
    key2: 3,
    obj2: {
        obj2Key1 : "value or inner object",
    }
}

Console.log(obj.obj2.obj2Key1);

let add = 1 + 2;
let sub = 2 - 1;
let mult = 2*4;
let division = 4 / 2;
let mod = 5 % 2;

let string = "String 1 " + "String 2";

console.log(string);